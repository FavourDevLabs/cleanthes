<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/cleanthes_surface"
    app:cardCornerRadius="16dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/cleanthes_border">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!--
            Icon container — 44dp circle with initial letter.
            The gold dot at top-right is the TOTP indicator.
            It is GONE when no TOTP is configured, so no space is wasted.
        -->
        <FrameLayout
            android:id="@+id/item_icon_container"
            android:layout_width="44dp"
            android:layout_height="44dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <View
                android:id="@+id/item_icon_bg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/shape_circle_accent" />

            <TextView
                android:id="@+id/item_tv_initial"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textColor="@android:color/white"
                android:textSize="18sp"
                android:textStyle="bold" />

            <!--
                TOTP indicator dot — 10dp gold circle at top-right corner.
                The 1.5dp black stroke is the visual separation from the avatar.
                Visibility set in VaultEntryAdapter.bind() via entry.hasTOTP().
            -->
            <View
                android:id="@+id/item_totp_dot"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_gravity="top|end"
                android:background="@drawable/shape_totp_dot"
                android:visibility="gone" />

        </FrameLayout>

        <TextView
            android:id="@+id/item_tv_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textColor="@color/cleanthes_text_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:ellipsize="end"
            android:maxLines="1"
            app:layout_constraintStart_toEndOf="@id/item_icon_container"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/item_btn_copy" />

        <TextView
            android:id="@+id/item_tv_username"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/cleanthes_text_secondary"
            android:textSize="13sp"
            android:ellipsize="end"
            android:maxLines="1"
            app:layout_constraintStart_toStartOf="@id/item_tv_title"
            app:layout_constraintTop_toBottomOf="@id/item_tv_title"
            app:layout_constraintEnd_toStartOf="@id/item_btn_copy" />

        <ImageButton
            android:id="@+id/item_btn_copy"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_copy"
            android:contentDescription="Copy password"
            app:tint="@color/citadel_gold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>

